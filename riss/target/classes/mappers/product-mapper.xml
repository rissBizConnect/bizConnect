<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
 "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="productMapper">

	<resultMap type="ProductDTO" id="productdto">
		<result property="ProductNo" column="PRODUCT_CD" />
	</resultMap>

	<select id="select" resultMap="productdto">
		select *
		from TB_PRODUCT
	</select>
	
	<insert id="insertproduct" parameterType="ProductDTO">
		INSERT INTO tb_product (PRODUCT_CD, COM_CODE, PRODUCT_NAME, PRODUCT_SIZE, PRODUCT_PRICE, PRODUCT_PURCHASE_PRICE) 
		VALUES(#{ProductNo}, #{LicenseCode}, #{ProductName}, #{ProductSize}, #{ProductPrice}, #{ProductPurPrice})
	</insert>

	

</mapper>










