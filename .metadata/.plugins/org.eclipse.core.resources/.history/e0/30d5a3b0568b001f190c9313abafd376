package org.riss.bizconnect.hr.document.model.service;

import java.util.List;

import org.riss.bizconnect.hr.document.model.dto.Certificate;

public interface CertificateService {
	   
	 int addCertificate(Certificate contract);
	 Certificate getCertificate(String contractId);
	 int updateCertificate(Certificate contract);
	 int deleteCertificate(String contractId);
	 List<Certificate> searchCertificate(String title);
}
